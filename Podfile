platform :ios, '10.0'
use_frameworks!

# common pods
def common_pods
    # pod 'DotsLoading', '~> 1.0.0'
    pod 'FillableLoaders', '~>1.3.0'
end

target 'MadridShops' do
    common_pods
end

target 'MadridShopsTests' do
    common_pods

end
